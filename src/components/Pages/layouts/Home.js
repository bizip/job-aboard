import React from 'react';

const propTypes = {};

const defaultProps = {};

const Home = () => (
  <div>
    <h1>Home Page</h1>
  </div>
);

Home.propTypes = propTypes;
Home.defaultProps = defaultProps;

export default Home;
